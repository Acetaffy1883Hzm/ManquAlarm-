# 更新记录

## 1.0.5

- 守候通知固定为前台服务通知，响铃使用另一条通知，结束响铃后继续保留守候状态。
- 通知提供立即检测、停止守候等操作，响铃通知提供停止与稍后提醒操作。
- 授权状态发生变化或回到应用时重新读取权限与通道状态，并更新界面。
- 设置页分别显示服务运行、系统授权、通道状态和通知登记情况，避免把服务运行误写成通知已显示。
- 停止守候直接撤销检测和相关定时任务；不再为了执行停止动作而新建服务。
- 前台服务临时启动失败时保留先前的守候设置；开启守候时的测试结束后继续正常检测。
- 保留 1.0.3 的显式同意兼容响铃，以及之前的铃声选择和设置即时刷新修复。

验证：30 个界面与模拟通信场景通过；13 个通知状态与 12 个响铃策略断言通过。原生测试 APK 已编译，尚未由本项目验证环境在真机执行；完整范围见 [测试记录](docs/TESTING.md)。

通知开关被设备管理策略固定拒绝时，需调整对应管理策略。本版识别并解释该状态，不宣称可以绕过它。

## 1.0.4

- 识别系统策略拒绝通知的状态，避免重复请求已被策略阻止的运行时授权。
- 增加用户主动导出的通知诊断与可选系统权限组件导出。

## 1.0.3

- 应用名称统一为“满区闹钟”。
- 新增“通知异常时仍响铃”，须由用户明确开启。
- 增加响铃时可选的悬浮停止入口，普通使用不依赖外部授权工具。

## 1.0.1–1.0.2

- 修复设置保存和异步状态刷新竞态：铃声、开关、主题等选择无需切页即可显示。
- 修复只有权限变化时界面不更新的问题，增强返回应用后的授权读取和系统设置入口。
