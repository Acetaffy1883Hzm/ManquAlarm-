# 验证范围

## 已完成

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 1.0.5 界面回归 | 30 个场景通过 | 模拟 Android 通信下的选择刷新、异步竞态、权限展示、兼容同意、时区和通知状态 |
| 1.0.5 守候通知状态 | 13 个断言通过 | 服务状态、授权、通知通道和登记状态的组合 |
| 1.0.5 响铃策略 | 12 个断言通过 | 正常授权、拒绝、不一致及显式同意声音兼容 |
| 核心时间与场次规则 | 36,038 个断言通过 | 时间边界、跨日、每周规则、时区、夏令时、场次去重与补报；本次公开打包重新执行通过 |
| 原签名发行 APK | 已构建并核验签名 | 包名、版本、资源与打包内容；摘要见 `APK-SHA256.txt` |

可公开的 1.0.5 自动化记录保存在 [`verification/1.0.5/`](../verification/1.0.5/)。运行方法见 [BUILDING](BUILDING.md)。

## 真机反馈的适用范围

测试者反馈此前兼容版本在小米可以播放声音，另有 Vivo 正常使用反馈。针对一份 1.0.5 系统报告，确认前台守候服务正在运行，并发现设备管理策略固定拒绝通知；这不能代表所有小米设备的原因或完整兼容性。

原生测试 APK 已完成编译，没有在本项目验证环境的真实手机上执行。浏览器模拟通信也不等于实际 Android 服务、声音或锁屏行为。

## 尚未完成

- 所有支持的 Android 版本与厂商机型上的安装、授权、锁屏响铃、通知操作和恢复测试。
- 解除设备管理策略后系统通知开关持续保持的真机复验。
- 长时间待机耗电、网络中断、系统杀进程、后台限制与真实开播端到端验证。
- 各厂商真实手机的原生版回归与整夜待机验证。

因此本项目不承诺零漏报、秒级必达或永久保活。


## 1.1.0 原生版

新增设备测试见 `app/src/androidTest/java/dev/hazel/livealarm/NativeUiTests.kt`。逐次结果、截图和报告以对应 Actions 构建为准；原有 Web 模拟测试不计入原生页面通过项。

[1.1.0 预览对应 CI](https://github.com/Acetaffy1883Hzm/ManquAlarm-/actions/runs/34700682902) 完成以下验证：

| 验证 | 结果 |
| --- | --- |
| Java 规则、通知与响铃策略 | 36,063 项断言通过 |
| APK 与设备测试 APK 编译 | 通过 |
| Android 15 原生交互与服务测试 | 7 项通过，无跳过 |
| 实际原生截图 | 首页、配色、背景、深色时段、动态深色和响铃页共 6 张 |
| APK 完整性 | Android 签名校验通过，附 SHA-256 |

7 项设备测试覆盖页面无 WebView、配色持久化、可撤销的最近任务隐藏、私有背景导入与移除、时段编辑与深色动态主题的实际文字对比度、服务恢复及外观变化不重启监听、停止守候后取消恢复，以及原生音频启动与停止流程（部分项目在同一测试内）。不包含真实手机整夜待机或真实开播计时。
